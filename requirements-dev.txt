# Development dependencies: the offline scripts, the test suite and the
# evaluation notebook. Deliberately NOT in requirements.txt — Streamlit
# Community Cloud installs that file, and pulling ~40 extra packages
# (jupyterlab, nbconvert, scikit-learn, matplotlib) into a deploy that never
# imports them costs minutes of build time and risks a timeout.
#
#     pip install -r requirements-dev.txt      # includes the runtime deps
#
# Verified by test: the app's import chain runs with every package below
# unimportable.

-r requirements.txt

# --- PDF, for scripts/build_vector_store.py ------------------------------
PyPDF2==3.0.1

# --- Tests ---------------------------------------------------------------
pytest==8.3.5

# --- Evaluation metrics and plots, used by tests/test_evals.ipynb --------
scikit-learn==1.6.1
matplotlib==3.10.0
seaborn==0.13.2

# --- Notebook ------------------------------------------------------------
# tests/test_evals.ipynb is a required deliverable, so the kernel that runs it
# is a real dependency of the project — just not of the deployed app.
jupyter==1.1.1
ipykernel==6.30.1
