# Runtime dependencies — everything the Streamlit app and the CLI need, and
# nothing else. This is the file Streamlit Community Cloud installs, so keeping
# it minimal is what keeps a deploy from timing out.
#
# The notebook, the tests and the offline scripts live in requirements-dev.txt.
# Install both for development:
#
#     pip install -r requirements-dev.txt
#
# ---------------------------------------------------------------------------
# PYTHON VERSION: 3.13. These pins come from the course (../Course24), and none
# of them publish wheels for 3.14 — tiktoken then tries to build from source and
# fails with "can't find Rust compiler", which is exactly how the first Cloud
# deploy died. On Community Cloud, pick 3.13 under "Advanced settings"; it
# cannot be changed after the app is created.
# ---------------------------------------------------------------------------
#
# LangChain is held at the 0.3.x line on purpose: the Course22 notebooks were
# authored against 0.3.x, and in LangChain 1.x two taught imports are already
# broken (`langchain.memory.ChatMessageHistory`, `langchain.output_parsers`).
# Do not bump without re-verifying every snippet in CLAUDE.md section 4.

# --- LangChain -----------------------------------------------------------
langchain==0.3.25
langchain-core==0.3.60
langchain-openai==0.3.17
langchain-community==0.3.24
langchain-text-splitters==0.3.8

# --- OpenAI --------------------------------------------------------------
openai==1.81.0
tiktoken==0.9.0

# --- Vector store --------------------------------------------------------
chromadb==1.0.12

# --- Database ------------------------------------------------------------
SQLAlchemy==2.0.41
pandas==2.2.3
numpy==2.1.3

# --- UI ------------------------------------------------------------------
streamlit==1.47.1

# --- Config --------------------------------------------------------------
python-dotenv==1.1.0
pydantic==2.11.7
