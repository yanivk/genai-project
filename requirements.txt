# Pinned to the stack the course actually ran (../Course24/requirements.txt).
#
# LangChain is held at the 0.3.x line on purpose: the Course22 notebooks were
# authored against 0.3.x, and in LangChain 1.x two taught imports are already
# broken (`langchain.memory.ChatMessageHistory`, `langchain.output_parsers`).
# Do not bump without re-verifying every snippet in CLAUDE.md section 4.

# --- LangChain -----------------------------------------------------------
langchain==0.3.25
langchain-core==0.3.60
langchain-openai==0.3.17
langchain-community==0.3.24
langchain-text-splitters==0.3.8

# --- OpenAI --------------------------------------------------------------
openai==1.81.0
tiktoken==0.9.0

# --- Vector store --------------------------------------------------------
chromadb==1.0.12

# --- PDF -----------------------------------------------------------------
PyPDF2==3.0.1

# --- Database ------------------------------------------------------------
SQLAlchemy==2.0.41

# --- UI ------------------------------------------------------------------
streamlit==1.47.1

# --- Data & evaluation ---------------------------------------------------
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.6.1
matplotlib==3.10.0
seaborn==0.13.2

# --- Config --------------------------------------------------------------
python-dotenv==1.1.0
pydantic==2.11.7

# --- Tests ---------------------------------------------------------------
pytest==8.3.5

# --- Notebook ------------------------------------------------------------
# tests/test_evals.ipynb is a required deliverable, so the kernel that runs it
# is a real dependency, not an optional dev extra.
jupyter==1.1.1
ipykernel==6.30.1
